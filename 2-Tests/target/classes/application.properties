spring.application.name=catalog
spring.profiles.active=dev
# Define se um EntityManager JPA ou uma Session Hibernate ser� mantido aberto
# no final de cada chamada � m�todos da service at� a fase de renderiza��o,
# ou seja, define se o processo � matido at� chegar no controller ou deve ser
# encerrado na service antes de retornar para o controller
spring.jpa.open-in-view=false
